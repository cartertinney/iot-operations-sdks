---
test-name: CommandExecutorRequestTopicModelIdWithoutReplacement_ThrowsException
aka:
- RequestTopicModelIdWithoutReplacementThrowsException
description:
  condition: >-
    CommandExecutor request topic contains a '{modelId}' token but no model ID is specified
  expect: >-
    CommandExecutor throws 'invalid configuration' exception.

prologue:
  executors:
  - request-topic: "mock/{modelId}/test"
    model-id:
  catch:
    error-kind: invalid configuration
    in-application: !!bool false
    is-shallow: !!bool true
    is-remote: !!bool false 
    supplemental:
      property-name: 'requesttopicpattern'
      property-value: "mock/{modelId}/test"
...
